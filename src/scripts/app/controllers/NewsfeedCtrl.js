angular.module('NewsfeedCtrl', [])
    .controller('NewsfeedController', ['$scope', function($scope) {
        'use strict';
        // Eventually, there will be stuff here to determine which part of the newsfeed should be active.
    }]);
